import colors from '../../utils/color';

const style = {
    contentView: {
        flexDirection: 'column',
        paddingTop: 120,
        marginBottom: 50,
    },
    title: {
        fontSize: 25,
        color: colors.blue,
        textAlign: 'center',
        marginBottom: 20,
    },
    scrollView: {
        
    }
}

export default style;
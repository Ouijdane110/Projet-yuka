import React from 'react';
import { Text, View } from 'react-native';
import screensView from '../../utils/globalStyle';

const DetailsBarCode = () => <View style={screensView}>
    <Text>(donné que l'on recupère après le scan)</Text>
</View>

export default DetailsBarCode;